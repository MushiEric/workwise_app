import 'package:flutter/material.dart';

/// Holds the app color palette.
class AppColors {
  AppColors._();

  // Primary / Secondary
  static const Color primary = Color.fromARGB(255, 0, 60, 255);   // Blue
  static const Color secondary = Color(0xFFFFC107); // Amber
  static const Color muted = Color(0xFF6C757D);     // Gray
  static const Color  purple = Color(0xFF8900FF);

  // Status colors
  static const Color success = Color(0xFF4CAF50); // Green
  static const Color error = Color(0xFFF44336);   // Red
  static const Color info = Color(0xFF2196F3); 
  static const Color warning = Color(0xFFFFC107);   // Amber
  static const Color primaryDark = Color(0xFF0039B5);

  // Common
  static const Color white = Colors.white;
  static const Color black = Colors.black;

  // Swatch
  static const MaterialColor primarySwatch = Colors.purple;

  //background
    static const Color surfaceVariantLight = Color(0xFFF1F5F9);

    
}
